import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    domains: ['scontent.fdel11-4.fna.fbcdn.net', 'scontent.cdninstagram.com'],
  },

};

export default nextConfig;
